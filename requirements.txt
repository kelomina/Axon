# Runtime dependencies. Keep this list aligned with pyproject.toml [project.dependencies].
torch>=2.1.0
numpy>=1.26.0
scikit-learn>=1.3.0
pandas>=2.0.0
pefile>=2023.2.7
matplotlib>=3.8.0
tqdm>=4.66.0

# Build tooling baseline matching pyproject.toml [build-system].
setuptools>=78.1.1
wheel
